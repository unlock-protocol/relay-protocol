{
  "name": "@relay-protocol/monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unlock-protocol/relay-protocol.git"
  },
  "bugs": {
    "url": "https://github.com/relay-protocol/unlock/issues"
  },
  "author": "contact@unlock-protocol.com",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/**",
    "smart-contracts",
    "backend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "packages": "yarn workspaces foreach --recursive --from '{@relay-protocol/types,@relay-protocol/networks,@relay-protocol/helpers}'",
    "packages:clean": "yarn packages --parallel run clean",
    "packages:build": "yarn packages --topological-dev run build",
    "build": "yarn packages:build",
    "clean": "yarn packages:clean"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "yarn@4.6.0",
  "dependencies": {
    "eslint": "9.18.0",
    "eslint-plugin-evm-address-to-checksummed": "0.0.6",
    "eslint-plugin-json": "4.0.1",
    "eslint-plugin-mocha": "10.5.0",
    "eslint-plugin-prettier": "5.2.3",
    "eslint-plugin-sort-keys-fix": "1.1.2",
    "husky": "9.1.7",
    "lint-staged": "15.4.1",
    "prettier": "3.4.2",
    "typescript-eslint": "8.21.0"
  }
}
